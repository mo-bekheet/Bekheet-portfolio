# Agentic Browsing — 1/2

This category is still experimental in PageSpeed Insights. It checks how well AI agents/crawlers can parse and interact with the page.

- [ ] **Accessibility tree is not well-formed** — likely stems from the same issues in `accessibility.md` (missing accessible names, bad landmark/heading structure, `aria-hidden` misuse on focusable elements). Fixing accessibility should resolve most of this too.

*1 audit passes; 4 are not applicable.*
